const socket = new SocketManager();
const pen = new Pen(document.getElementById("canvas"));
const input = new InputManager({
    onMouseDown: (event) => {
        
    },
    onMouseMove: (event) => {
        
    },
    onMouseUp: (event) => {
    
    },
});
